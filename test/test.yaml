apiVersion: tf.upbound.io/v1beta1
kind: Workspace
metadata:
  name: sample-remote
spec:
  forProvider:
    module: https://app.terraform.io/dramisinfo/vnet/azurerm
    source: Remote
  providerConfigRef:
    name: platform-api
    namespace: crossplane-system
